version: "3"

services: 
    db:
        container_name: mongo_container
        image: mongo
        restart: always
        ports: 
            - "27017:27017"
        volumes: 
            - ./data/db:/data/db
